{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile cpp",
      "type": "shell",
      "command": "g++",
      "args": ["-g", "${file}"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "execute",
      "type": "shell",
      "command": "${fileDirname}./${fileBasenameNoExtension}"
    }
  ]
}
